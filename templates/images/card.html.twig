
<div class="col">
    <a href="/link/{{r.id}}">
    <div class="card">
      {# <div class="card-header">{{r.title}}</div> #}

      <div class="card-body p-0">
        <img src="{{r.url|default('/img/thumbnail_not_found.jpg')}}" class="card-img-top" alt="{{r.title}}">
      </div>

      <div class="card-footer">
        {{r.title}}
        {# discussion:{{r.DiscussionId}} #}
      </div>

    </div>
    </a>
</div>
